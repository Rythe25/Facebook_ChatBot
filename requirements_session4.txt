# Session 4 — booking action: write the order/reservation into Google Sheets.
# Adds on top of Sessions 1-3. Install all four to set up a fresh machine:
#   pip install -r requirements.txt
#   pip install -r requirements_session2.txt
#   pip install -r requirements_session3.txt
#   pip install -r requirements_session4.txt
#
# gspread talks to the Sheets API; it pulls in google-auth itself, which reads
# the service-account JSON at app/credentials/service_account.json.
# Remember to share the spreadsheet with that account's client_email (Editor).

gspread

# python-dotenv is only needed for standalone scripts that read .env directly;
# the app itself loads .env through pydantic-settings (Session 1).
python-dotenv
